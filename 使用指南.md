# new-api CCS 导入助手 — Chrome 使用指南

本文档介绍如何在 Chrome 浏览器中安装并使用「new-api CCS 导入助手」插件，将 new-api 渠道一键导入 CC Switch 并自动配置用户额度查询。

## 一、准备工作

在开始之前，请确认已具备以下条件：

| 准备项 | 说明 |
|---|---|
| Chrome 浏览器 | 亦支持 Edge（`edge://extensions`） |
| CC Switch 桌面应用 | 版本 ≥ 3.13（支持用量查询）；需已注册 `ccswitch://` 协议（便携版换版本后协议可能失效） |
| new-api 后台账号 | 需能正常登录目标站点后台 |
| 令牌 apikey | `sk-` 开头的令牌 key，在 new-api 后台「令牌」页创建/复制 |

## 二、安装插件（开发者模式）

1. 获取插件代码（克隆仓库或直接使用本地文件夹 `E:\workspace\new-api-ccs-extension`）
2. 打开 Chrome，地址栏输入 `chrome://extensions` 回车
3. 打开右上角的「**开发者模式**」开关
4. 点击左上角「**加载已解压的扩展程序**」
5. 选择插件所在文件夹，点击「选择文件夹」
6. 安装完成后，扩展列表中会出现「**new-api CCS 导入助手**」，固定到工具栏（可选，插件无需点击图标使用）

> 权限说明：插件会提示「读取所有网站数据」，原因是 new-api 部署域名不固定；插件只在检测到 new-api 后台特征时才会注入按钮，凭据仅保存在浏览器本地。

## 三、日常使用步骤

1. **登录 new-api 后台**：用浏览器打开目标站点并完成登录
2. **唤起插件**：页面右下角出现紫色「**导入CCS**」悬浮按钮，点击它
3. **选择应用**：在弹窗中选择 Claude 或 Codex（Gemini 暂时隐藏）
4. **确认默认值**：
   - 名称：已默认填好「深夜API」
   - 模型：Claude 默认 `claude-opus-5`，Codex 默认 `gpt-5.5`（可手输或下拉修改）
5. **填写 apikey**：粘贴你的令牌 key（`sk-...`）
6. **开始导入**：点击「开始导入」按钮
7. **允许协议唤起**：浏览器弹出「打开 CC Switch？」提示时，点击「打开」（可勾选"始终允许"）
8. **在 CC Switch 中确认**：CC Switch 会弹出导入确认框，展示供应商配置与用量查询脚本，点击确认导入
9. **手动启用**：导入后的 provider 默认处于**未启用**状态，在 CC Switch 中点击切换到该供应商后开始使用

## 四、验证效果

- **切换使用**：在 CC Switch 中切换到「深夜API」，启动 Claude Code / Codex 即通过 new-api 中转
- **余额显示**：卡片启用后，底部显示该**用户账号**的余额条（500000 quota = 1 USD 换算），每 5 分钟自动刷新；也可点刷新图标立即查询
- **登录检测**：弹窗状态区显示绿色「已登录：用户 #id ｜ 余额 $xx / 已用 $xx」即表示插件与站点会话连接正常

## 五、常见问题排查

| 现象 | 处理办法 |
|---|---|
| 页面没有悬浮按钮 | 确认当前是 new-api 后台页面；刷新页面；检查扩展是否已启用 |
| 弹窗提示「登录检测失败」 | 确认已登录后台；点击弹窗「诊断」按钮，将输出反馈给开发者 |
| 点击导入后无反应 | 确认 CC Switch 已安装且 `ccswitch://` 协议有效（便携版换版本后需重注册协议）；检查浏览器是否拦截了外部协议弹窗 |
| 卡片余额不显示 | 卡片需处于启用状态才会自动刷新；确认站点换算比（QuotaPerUnit）未被修改 |
| 导入后 provider 未启用 | 属正常行为（`enabled=false`），在 CC Switch 中手动切换即可 |
| 需要更换访问令牌 | 弹窗底部「重新生成访问令牌」需两步确认；注意会作废旧 AT（若有其他工具在用旧 AT 会失效） |

## 六、更新插件

1. 更新代码后，回到 `chrome://extensions`
2. 点击插件卡片上的**刷新图标**
3. 回到 new-api 页面**刷新页面**（让新代码生效）
4. 确认弹窗顶部版本号已更新

## 七、卸载

`chrome://extensions` → 找到「new-api CCS 导入助手」→ 点击「移除」。卸载后所有本地缓存的凭据一并删除。
